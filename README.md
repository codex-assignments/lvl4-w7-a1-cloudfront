# lvl4-w7-a1-cloudfront

# built with CLI npm create vite@latest

# to compile the frontend for production, CLI npm run build

this will create a /dist folder with minified, bundled js

# to deploy: uploaded the /dist folder to a bucket in AWS S3, then created a distribution on AWS Cloudfront linked to that bucket

live URL: dqyua3h3xoave.cloudfront.net
