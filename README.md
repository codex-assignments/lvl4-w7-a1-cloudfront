# lvl4-w7-a1-cloudfront

# built with CLI npm create vite@latest

# to compile the frontend for production, CLI npm run build

this will create a /dist folder with assets and minified, bundled older js that is understood by the browser

# to deploy: uploaded the /dist folder to a bucket in AWS S3, then created a distribution on AWS Cloudfront linked to that bucket as its origin

live URL: d3i9yo7qgmug9f.cloudfront.net
